
<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Restaurant Review</title>
  </head>
  <body>
      <div class="container">
          <div class="well">
              <h1>Burnt Toast</h1>
              <p>A Restaurant ood guide for people Who think they know how to run a restaurant  </p>
          </div>
          <div class="col col-xs-2">
              <h1>Search By Cuisine</h1>
              {% if cuisines is not empty %}
                {% for cuisine in cuisines %}
                <a href="/cuisine/{{cuisine.id}}"><p>{{cuisine.type}}</p></a>
                {% endfor%}
              {% endif %}
              <p>If your cuisine is not listed, enter it in here </p>
              <form action='/cuisines' method='post'>
                <label for='type'></label>
                <input type='text' id='type' name='type'>
                <button type='submit'>Submit</button>
          </div>
          <div class="col col-xs-7">
              <h1 class = "featuredRestaurants">{{cuisine.type}}</h1>
              {% if restaurants is not empty %}
                {% for restaurant in restaurants %}
                <p> <a href="/restaurant/{{restaurant.id}}">{{restaurant.name}}</a>
                   Type of Restaurant: <span>{{cuisine.type}}</span> Neighborhood: <span>{{restaurant.neighborhood}}</span>
                  Must Eats: <span>{{restaurant.must_eats}}</span>
                  Price Range:<span>{{restaurant.price_range}}</span></p>
                {% endfor %}
              {% endif %}
              <h1>Restaurants</h1>
              <form class="" action="index.html" method="post">
              </form>
              <form action="/cuisine/{{cuisine.id}}" method='post'>
                <input type="hidden" name="cuisine_id" value="{{cuisine.id}}">
                <label for='type'>Name</label>
                <input type='text' name='name'>

                <label for='type'>Neighborhood</label>
                <input type='text' name='neighborhood'>

                <label for='type'>MustEats</label>
                <input type='text' name='must_eats'>

                <label for='type'>Price Range</label>
                <input type='text' name='price_range'>
                <br>
                <button type='submit'>Submit</button>
                <br>
                <a href="/"><button type="button" name="button">Home</button></a>
              </form>

          </div>
          <div class="col col-xs-3">
              <h1>Top Rated Restaurants</h1>
                <ol>
                    {% if topRestaurants is not empty %}
                      {% for restaurant in topRestaurants %}
                        <li><a href="/restaurant/{{restaurant.id}}"><p>{{restaurant.name}}</p></a></li>
                      {% endfor%}
                    {% endif %}
                </ol>
          </div>
      </div>


    </form>
  </body>
</html>
